# Migration `20201103184039`

This migration has been generated by Eranda Bandaranaike at 11/4/2020, 12:10:39 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Videos` (
`id` int  NOT NULL  AUTO_INCREMENT,
`created_at` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`content` json  ,
`published` boolean  NOT NULL DEFAULT false,
`author_id` int  NOT NULL ,
`thumbnail` varchar(191)  ,
`video_url` varchar(191)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Users` (
`id` int  NOT NULL  AUTO_INCREMENT,
`email` varchar(191)  NOT NULL ,
`name` varchar(191)  ,
`password` varchar(191)  NOT NULL ,
UNIQUE INDEX `Users.email_unique`(`email`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

DROP TABLE `users`

DROP TABLE `videos`

DROP TABLE `_migration`

ALTER TABLE `Videos` ADD FOREIGN KEY (`author_id`) REFERENCES `Users`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201103182006-first..20201103184039
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,9 +13,9 @@
 model Videos {
   id         Int      @id @default(autoincrement())
   created_at DateTime @default(now())
   title      String
-  content    String?
+  content    Json?
   published  Boolean  @default(false)
   author     Users    @relation(fields: [author_id], references: [id])
   author_id  Int
   thumbnail  String?
```


